# GanttMD 可视化 V2 需求

## 一、整体布局

### 顶部区域（不变）
- 项目名称 + 统计摘要（总任务、已完成、进行中、可执行、被阻塞）
- 里程碑进度横条（M1→M2→M5→M7，当前阶段高亮）

### 中部区域（核心改动）
- **视角切换 Tab**：支持多种分组视角查看同一批任务
- **任务卡片区**：按当前视角分组展示

### 底部区域
- 移除原有的「依赖关系总览」面板
- 依赖关系通过卡片交互来呈现（见第四节）

## 二、视角切换

提供至少两种分组视角，通过 Tab 切换：

| 视角 | 分组依据 | 适用场景 |
|------|---------|---------|
| 里程碑视角 | 按 milestone（M1/M2/M5/M7）分组 | 看当前阶段做了什么、还差什么 |
| 模块视角 | 按模块（后端工程主线/模块规格主线）分组 | 看某个功能域的完成度 |

每个分组显示：分组名称 + 完成进度（如 `6/9 · 67%`）。

组内任务按拓扑排序（依赖在前，被依赖在后）。

## 三、任务卡片内容

每张卡片展示以下信息：

```
┌─────────────────────────┐
│ S-BE-03                 │
│ 创建后端工程基础结构       │
│                         │
│ [已完成]  [后端工程主线]   │
│ ↑1 阻塞  ↓3 依赖我  [👁] │
└─────────────────────────┘
```

### 字段说明

| 字段 | 说明 |
|------|------|
| 任务 ID | 如 `S-BE-03` |
| 任务标题 | 如 `创建后端工程基础结构` |
| 状态标签 | 已完成 / 进行中 / 可执行 / 被阻塞 |
| 模块标签 | 所属模块名称 |
| ↑N 阻塞 | 该任务有 N 个前置依赖（上游）尚未完成 |
| ↓N 依赖我 | 有 N 个下游任务直接依赖该任务 |
| 👁 按钮 | 点击后触发依赖高亮交互（见第四节） |

### 显示规则

- `↑N 阻塞`：N = 未完成的上游依赖数量。若 N=0 则不显示此标签。
- `↓N 依赖我`：N = 直接依赖该任务的下游任务数量。若 N=0 则不显示此标签。
- 不在卡片内列出具体的依赖任务 ID。

## 四、依赖高亮交互

### 触发方式

点击任务卡片上的「👁」按钮（或整个卡片可点击）。

### 交互效果

1. **当前卡片**：保持原样或加边框强调。
2. **上游依赖**（该任务依赖的所有任务，递归向上）：高亮为**蓝色**调。
3. **下游依赖**（依赖该任务的所有任务，递归向下）：高亮为**橙色**调。
4. **其他所有卡片**：降低透明度（变暗/变灰），退到背景。
5. **再次点击**同一个按钮或点击空白区域：取消高亮，恢复所有卡片。

### 高亮范围

- 上游：沿 `dependencies` 向上递归查找所有祖先任务。
- 下游：沿反向依赖向下递归查找所有后代任务。
- 不限于同一里程碑/模块分组，跨组的依赖卡片也要高亮。

### 视觉示意

```
正常状态：  [A] → [B] → [C] → [D] → [E]

点击 C 的 👁 后：

  上游（蓝色）    当前      下游（橙色）
  [A]  →  [B]  →  [C]  →  [D]  →  [E]
                    ↑
                  高亮中心

  其他无关卡片全部变暗
```

## 五、不做（本轮）

- 不做路线图/甘特时间轴视图
- 不做依赖连线（SVG 箭头画线）
- 不做拖拽排序
- 不做右侧面板
- 不做任务编辑功能
