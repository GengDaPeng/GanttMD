# 校验脚本检查流程图

本文说明 `ganttmd validate` 会读取什么、检查什么，以及最后输出什么。

校验脚本只读 `.ganttmd/` 和被 `source_docs` 引用的路径是否存在，不会修改任何文件。

## 1. 运行入口

在使用方项目根目录运行：

```bash
ganttmd validate
```

如果从其他目录执行，也可以显式传项目路径：

```bash
ganttmd validate /path/to/project
```

也可以直接传 `.ganttmd/` 目录：

```bash
ganttmd validate /path/to/project/.ganttmd
```

如需机器读取结果：

```bash
ganttmd validate --json
```

## 2. 整体流程

```mermaid
flowchart TD
  A[开始: ganttmd validate] --> B[解析参数]
  B --> C{传入路径是 .ganttmd 吗}
  C -- 是 --> D[项目根目录 = .ganttmd 的父目录]
  C -- 否 --> E[项目根目录 = 传入路径]
  D --> F[定位 ganttRoot]
  E --> F[定位 ganttRoot = project/.ganttmd]

  F --> G[读取 config.yaml]
  F --> H[扫描 tasks/*.md 与 modules/*.md]
  F --> I[读取 followups.md]
  F --> I2[读取 runs.md]

  G --> J[解析里程碑配置]
  H --> K[抽取 ganttmd-task 代码块]
  I --> L[抽取 ganttmd-followup 代码块]
  I2 --> L2[抽取 ganttmd-run 代码块]

  J --> M[项目级初始化检查]
  K --> N[任务 ID 索引与重复检查]
  N --> O[计算依赖派生字段]
  O --> P[逐个任务运行共享规则]
  L --> Q[逐个 follow-up 运行共享规则]
  L2 --> Q2[逐个 run 运行共享规则]
  H --> Q3[抽取 ganttmd-checklist 并运行规则]

  M --> R[合并 issues]
  P --> R
  Q --> R
  Q2 --> R
  Q3 --> R

  R --> S{是否 --json}
  S -- 是 --> T[输出 JSON]
  S -- 否 --> U[输出人类可读摘要与列表]
  T --> V{是否存在 warn}
  U --> V
  V -- 有 warn --> W[退出码 1]
  V -- 无 warn --> X[退出码 0]
```

## 3. 检查项分类图

```mermaid
mindmap
  root((validate 检查项))
    项目初始化
      是否存在 .ganttmd 目录
      是否存在 config.yaml
      是否存在 tasks 或 modules 文件
      任务文件是否能解析出 ganttmd-task
    任务结构
      是否缺 id
      id 是否重复
      是否缺 title
      是否缺 status
      status 是否非法
      kind 是否非法
      dependencies 是否指向不存在任务
    视图挂载
      是否缺 track
      track 是否非法
      track 旧别名提示
      是否缺 milestone
      milestone 是否未登记在 config
    协作安全
      in_progress 是否缺 owner 或 agent
      owner 与 agent 是否冲突
      review 是否缺 review_status
      review 是否超过 7 天未更新
      cancelled 是否缺取消原因或处理结论
    阻塞与闭环
      显式 blocked 是否缺 blocked_reason
      done 是否缺 evidence
      工程类 done 是否缺 verification
      done 或 cancelled 是否超过 7 天归档阈值
    来源与可执行性
      source_docs 是否缺失
      source_docs 路径是否不存在
      next_action 是否缺失
      acceptance 是否缺失
      多下游任务是否缺 downstream_constraints
    Follow-up
      必填字段是否缺失
      status 是否非法
      kind 是否非法
      PR 来源是否缺 source_pr 或 source_rr
      决策事项是否缺 decision_owner
      延期或外部等待是否缺 next_review_at
      是否超过 next_review_at
      accepted 是否缺 accepted_by accepted_at decision
      converted 是否缺 converted_task 或 resolution
      done/wontfix 是否缺 resolution
    Runs
      是否缺 id title status owner branch worktree
      active run 是否缺 owner
      run 引用任务是否不存在
      同一未完成任务是否出现在多个 active run
    Checklist
      是否缺 task_id
      是否引用不存在任务
      item 是否无法解析
      item status 是否非法
      父任务 done 时 checklist 是否仍有未完成项
      父任务 done/cancelled 时是否仍保留 checklist 待收口
```

## 4. 输出结果

普通输出包含：

```text
GanttMD 校验：<ganttRoot>
任务 <taskCount> 个，follow-up <followupCount> 个，run <runCount> 个，checklist <checklistCount> 个，警告 <warningCount> 个，提示 <infoCount> 个。
- 警告 <id> [<field>] <sourceFile>：<message>
- 提示 <id> [<field>] <sourceFile>：<message>
```

JSON 输出包含：

```json
{
  "root": "/path/to/project",
  "ganttRoot": "/path/to/project/.ganttmd",
  "taskCount": 5,
  "followupCount": 2,
  "runCount": 1,
  "checklistCount": 2,
  "issueCount": 0,
  "warningCount": 0,
  "issues": []
}
```

每条 `issue` 的核心字段：

| 字段 | 含义 |
| --- | --- |
| `level` | `warn` 或 `info` |
| `id` | 任务 ID、follow-up ID，或 `(project)` |
| `message` | 具体问题说明 |
| `sourceFile` | 问题来自哪个 `.ganttmd/` 文件 |
| `field` | 问题对应字段 |

## 5. 退出码规则

| 情况 | 退出码 | 含义 |
| --- | --- | --- |
| 存在 `warn` | `1` | 结构性问题，需要修复或由看板维护者确认 |
| 只有 `info` | `0` | 可运行，但有质量提示 |
| 没有 issue | `0` | 未发现结构问题 |

## 6. 当前不会做的事

- 不会自动修复任务文件。
- 不会自动归档 `done` 或 `cancelled` 任务；看板维护者需要显式补 `archived_at` / `archived_reason`。
- 不会证明 Agent 真的阅读过 `source_docs`。
- 不会校验 PR、commit 或测试命令是否真实存在。
- 不会替代页面视觉检查；它只负责结构性校验。
